<% if @error.nil? then %>
  <h1>Trip <%= @trip.id %></h1>
  <div><%= "Train: " + @trip.train.name %></div>
  <div><%= "User: " + @trip.user.name %></div>
  <div><%= "Date: " + @trip.date %></div>
<% else %>
  <%= @error %>
<% end %>